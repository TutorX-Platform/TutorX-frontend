export interface Payment {
  questionTitle: string;
  payRefNo: string;
  payStatus: string;
  questionId: string;
  fee: number;
  paidBy: string;
  paidTime: number;
  paidCurrency: string;
  tutorId: string;
  studentName: string;
  studentImage: string;
  tutorName: string;
  questionNumber: string;
}
